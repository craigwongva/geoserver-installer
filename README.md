http://geoserver..net/geoserver/web/

This server belongs to so-and-so according to the GUI front page,
so this particular download is not clean.

Internal validation:
sudo service httpd status
sudo service geoserver status
